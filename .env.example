# Server Configuration
NODE_ENV=development
PORT=5000

# Database Configuration (PostgreSQL)
# For Render/Railway: Use the provided DATABASE_URL from their dashboard
DATABASE_URL="postgresql://username:password@localhost:5432/storck_tours?schema=public"

# JWT Configuration
# IMPORTANT: Change these in production!
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this-in-production
JWT_REFRESH_EXPIRES_IN=7d

# Cookie Configuration
COOKIE_SECRET=your-cookie-secret-change-this-in-production

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS - Set to your frontend URL
# For multiple origins, separate with commas: http://localhost:3000,https://yourapp.com
CORS_ORIGIN=http://localhost:3000

# Admin Registration Secret (for registering admin users via API)
# IMPORTANT: Change this in production!
ADMIN_SECRET=storck-admin-secret-2024

# Cloud Storage (S3-compatible) - Configure when ready
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=
# AWS_S3_BUCKET=

# ============================================
# PRODUCTION DEPLOYMENT NOTES:
# ============================================
# 
# For Render.com:
#   - Set NODE_ENV=production
#   - Use their PostgreSQL and set DATABASE_URL
#   - All env vars go in Dashboard > Environment
#
# For Railway.app:
#   - Set NODE_ENV=production
#   - They auto-provision PostgreSQL
#   - Env vars in Settings > Variables
#
# For Fly.io:
#   - Use: flyctl secrets set KEY=VALUE
#   - Or set in fly.toml
#
# SECURITY CHECKLIST:
#   [ ] Change JWT_SECRET (use: openssl rand -hex 32)
#   [ ] Change JWT_REFRESH_SECRET
#   [ ] Change COOKIE_SECRET
#   [ ] Change ADMIN_SECRET
#   [ ] Set proper CORS_ORIGIN
#   [ ] Set NODE_ENV=production
# ============================================
